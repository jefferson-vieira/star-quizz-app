import React from 'react';

const PageHeader = ({ title }) => (
  <header className="page-header border-warning border-bottom pb-2 my-3">
    <h1>{title}</h1>
  </header>
);

export default PageHeader;
